import {describe,expect,it} from "vitest";
import {createBackup,toCsv,validateBackup} from "./backup";
describe("backup",()=>{it("gera e revalida envelope versionado",()=>{const backup=createBackup("user-1",null,{content_items:[{title:"A",views:null,sales:0}]},new Date("2026-09-04T12:00:00Z"));expect(validateBackup(JSON.parse(JSON.stringify(backup)))).toBe(true);expect(backup.entities.content_items).toEqual([{title:"A",views:null,sales:0}])});it("gera CSV seguro com BOM e escape",()=>{const csv=toCsv([{title:'A, "B"',zero:0,missing:null}]);expect(csv.startsWith("\uFEFF")).toBe(true);expect(csv).toContain('"A, ""B"""');expect(csv).toContain('"0"');expect(csv.endsWith(',""')).toBe(true)});
it("neutraliza fórmulas maliciosas (CSV injection) prefixando com apóstrofo",()=>{const csv=toCsv([{notes:'=HYPERLINK("http://evil","x")'},{notes:'+1+1'},{notes:'-1'},{notes:'@SUM(1,1)'},{notes:'texto normal'}]);expect(csv).toContain('"\'=HYPERLINK(""http://evil"",""x"")"');expect(csv).toContain('"\'+1+1"');expect(csv).toContain('"\'-1"');expect(csv).toContain('"\'@SUM(1,1)"');expect(csv).toContain('"texto normal"')})});
